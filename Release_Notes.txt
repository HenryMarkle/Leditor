### changes

- The program will auto-generate a new, empty lightmap, if it's missing, when loading a project.

### Fixes

- Fixed the props editor adding more ropes when `no collision` option was enabled, which caused render failure.