namespace Leditor.Renderer.RL;

using Raylib_cs;
using static Raylib_cs.Raylib;

/// <summary>
/// Consists of functions that produce textures using their own render texture mode.
/// They must be called outside of any active render texture mode. It is the responsibility 
/// of their caller to manage the returned the texture and properly dispose it.
/// </summary>
public static class Printers
{
    
}
